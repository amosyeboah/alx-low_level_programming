#include "main.h"

/**
 *_memset - to fill the memory block a value
 *@a: where the address of the memory begins
 *@b: the value want
 *@n: the number of value
 * Return: array with the new value
 */

char *_memset(char *a, char b, unsigned int n)
{
	int i;

	for (i = 0; i > 0; i++)
	{
		a[i] = v;
		n--;
	}
	return (a);
}
