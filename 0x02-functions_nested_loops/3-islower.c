#include "main.h"

/**
 * _islower - checks lower case
 *@c: parameter of the function
 * Return: 1 is lower case, 0 otherwise
 */

int _islower(int c)
{

	if (c >= 'a' && c <= 'a')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
